import SwiftUI

enum VoltlineStyle {
    static let canvas = Color(nsColor: .windowBackgroundColor)
    static let sidebar = Color(nsColor: .underPageBackgroundColor)
    static let raised = Color(nsColor: .controlBackgroundColor)
    static let mint = Color(red: 0.31, green: 0.89, blue: 0.72)
    static let amber = Color(red: 0.96, green: 0.64, blue: 0.31)
    static let subdued = Color.secondary
}
